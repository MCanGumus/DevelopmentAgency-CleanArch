@using DA.Domain.Dtos
@model List<EducationalInstitutionDto>

<table class="table dataTable table-striped table-hover">
    <thead>
        <tr>
            <th>Sınav Adı</th>
            <th class="text-center">İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count != 0)
        {
            @foreach (EducationalInstitutionDto item in Model)
            {
                <tr data-id="@item.Id.ToString()">
                    <td>@item.Name</td>
                    <td>
                        <a onclick="AjaxMethod('EducationalInstitutions/OpenModal', '@item.Id.ToString()', 'Update')" href=""><i class="mdi mdi-table-edit text-success md20"></i></a>
                        <a onclick="AjaxMethod('EducationalInstitutions/Delete', '@item.Id.ToString()', 'Delete')" href=""><i class="mdi mdi-delete text-danger md20"></i></a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

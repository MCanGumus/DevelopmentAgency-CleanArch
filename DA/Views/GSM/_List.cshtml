@using DA.Domain.Dtos
@using DA.Domain.Entities
@using DA.Domain.Enums
@model List<GSMNumberDto>

<table class="table dataTable table-striped table-hover">
    <thead>
        <tr>
            <th>Telefon Numarası</th>
            <th class="text-center">İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count != 0)
        {
            @foreach (GSMNumberDto item in Model)
            {
                <tr id="@item.Id.ToString()">
                    <td>@item.GSM</td>
                    <td>
                        <a onclick="AjaxMethod('GSMNumbers/OpenModal', '@item.Id.ToString()', 'Update')" href=""><i class="mdi mdi-table-edit text-success md20"></i></a>
                        <a onclick="AjaxMethod('GSMNumbers/Delete', '@item.Id.ToString()', 'Delete')" href=""><i class="mdi mdi-delete text-danger md20"></i></a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
